---
import { SITE_DATA } from '../data/siteData';

interface Props {
  currentPath?: string;
}

const { currentPath = '/' } = Astro.props;
const whatsappLink = SITE_DATA.getWhatsAppLink();
---

<header class="site-header" id="mainHeader">
  <div class="header-inner container-wide">
    <!-- Brand Crest & Title -->
    <a href="/" class="brand-link" aria-label="Elite School of Hospitality Management Home">
      <img src="/images/brand/logo.jpg" alt="Elite School of Hospitality Management Crest" class="brand-crest-img" width="46" height="46" />
      <div class="brand-text">
        <span class="brand-title">ELITE</span>
        <span class="brand-tagline">School of Hospitality Management</span>
      </div>
    </a>

    <!-- Desktop Navigation: Flat, Clean, No Pill Badges -->
    <nav class="desktop-nav" aria-label="Primary Navigation">
      <a href="/#courses" class={`nav-link ${currentPath === '/#courses' ? 'active' : ''}`}>Programs</a>
      <a href="/courses/dca" class={`nav-link ${currentPath === '/courses/dca' ? 'active' : ''}`}>Culinary Arts (DCA)</a>
      <a href="/#methodology" class="nav-link">Why Elite</a>
      <a href="/#practical" class="nav-link">Experience</a>
      <a href="/#campus" class="nav-link">Campus</a>
    </nav>

    <!-- Header Actions: Direct Phone + Architectural WhatsApp Button -->
    <div class="header-actions">
      <a href={`tel:${SITE_DATA.institution.primaryPhone.replace('-', '')}`} class="header-phone">
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
        </svg>
        <span>{SITE_DATA.institution.primaryPhone}</span>
      </a>

      <button type="button" class="btn-header-inquire" data-open-inquiry-modal aria-haspopup="dialog">
        Inquire
      </button>

      <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="btn-header-wa" aria-label="WhatsApp admissions chat">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12.031 6.172c-3.181 0-5.767 2.586-5.768 5.766-.001 1.298.38 2.27 1.019 3.287l-.582 2.128 2.182-.573c.976.58 2.029.924 3.149.925h.005c3.182 0 5.767-2.587 5.768-5.766.001-3.187-2.575-5.767-5.773-5.767zm0 10.375c-.981 0-1.898-.284-2.697-.79l-.193-.115-1.393.365.372-1.357-.126-.201c-.552-.881-.844-1.867-.843-2.88.001-2.535 2.062-4.597 4.6-4.597 2.536 0 4.601 2.061 4.601 4.597 0 2.537-2.064 4.598-4.598 4.598zm2.525-3.447c-.138-.07-.82-.405-.947-.451-.128-.046-.221-.07-.314.07-.093.139-.36.452-.441.545-.081.093-.163.105-.302.035-.139-.07-.588-.217-1.12-.692-.414-.37-.693-.827-.775-.966-.081-.139-.009-.215.061-.284.063-.062.139-.163.209-.244.07-.081.093-.139.139-.232.047-.093.023-.174-.012-.244-.035-.07-.314-.755-.43-1.034-.113-.271-.228-.234-.314-.239l-.268-.005c-.093 0-.244.035-.371.174-.128.139-.488.476-.488 1.16 0 .685.499 1.346.569 1.439.07.093.982 1.5 2.38 2.104.333.144.593.229.796.294.335.106.639.091.88.055.269-.04.82-.336.936-.661.116-.325.116-.603.081-.661-.035-.059-.128-.094-.267-.163z"/>
        </svg>
        <span>WhatsApp</span>
      </a>

      <!-- Mobile Hamburger -->
      <button type="button" class="mobile-toggle" id="mobileMenuToggle" aria-label="Toggle navigation menu" aria-expanded="false">
        <span class="bar"></span>
        <span class="bar"></span>
      </button>
    </div>
  </div>

  <!-- Mobile Drawer -->
  <div class="mobile-drawer" id="mobileDrawer">
    <div class="mobile-drawer-content">
      <nav class="mobile-nav">
        <a href="/#courses" class="mobile-nav-link">All Programs</a>
        <a href="/courses/dca" class="mobile-nav-link">Diploma in Culinary Arts (DCA)</a>
        <a href="/#methodology" class="mobile-nav-link">Why Elite</a>
        <a href="/#practical" class="mobile-nav-link">Kitchen Training (60+ Days)</a>
        <a href="/#video-gallery" class="mobile-nav-link">Inside Elite Reels</a>
        <a href="/#internship" class="mobile-nav-link">Internship & ODC</a>
        <a href="/#reviews" class="mobile-nav-link">Student Reviews</a>
        <a href="/#campus" class="mobile-nav-link">Location & Visit</a>
      </nav>

      <div class="mobile-actions">
        <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="btn-cib-solid w-full text-center">
          Chat on WhatsApp
        </a>
        <button type="button" class="btn-cib-outline w-full text-center" data-open-inquiry-modal>
          Inquire / Book Campus Visit
        </button>
      </div>

      <div class="mobile-footer">
        <p class="mobile-address">Pipalbot, Old Baneshwor, Devkota Sadak, Kathmandu</p>
        <p class="mobile-contact">Call: {SITE_DATA.institution.primaryPhone} / {SITE_DATA.institution.mobilePhone}</p>
      </div>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    background-color: #FFFFFF;
    border-bottom: 1px solid var(--color-border);
    transition: background-color var(--transition-normal), box-shadow var(--transition-normal);
  }

  .site-header.scrolled {
    background-color: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(12px);
    box-shadow: 0 4px 20px rgba(11, 19, 43, 0.04);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 76px;
  }

  /* Brand */
  .brand-link {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    text-decoration: none;
  }

  .brand-crest-img {
    width: 44px;
    height: 44px;
    object-fit: cover;
    border-radius: var(--radius-xs);
    border: 1px solid var(--color-border);
  }

  .brand-text {
    display: flex;
    flex-direction: column;
  }

  .brand-title {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 1.25rem;
    line-height: 1;
    letter-spacing: 0.08em;
    color: var(--color-navy);
  }

  .brand-tagline {
    font-family: var(--font-body);
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-text-secondary);
    margin-top: 2px;
  }

  /* Desktop Nav */
  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 2.25rem;
  }

  .nav-link {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-navy);
    letter-spacing: 0.02em;
    position: relative;
    padding: 0.4rem 0;
    transition: color var(--transition-fast);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0%;
    height: 2px;
    background-color: var(--color-orange);
    transition: width var(--transition-normal);
  }

  .nav-link:hover {
    color: var(--color-orange);
  }

  .nav-link:hover::after,
  .nav-link.active::after {
    width: 100%;
  }

  /* Header Actions */
  .header-actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }

  .header-phone {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-navy);
    transition: color var(--transition-fast);
  }

  .header-phone:hover {
    color: var(--color-orange);
  }

  .btn-header-inquire {
    font-family: var(--font-display);
    font-size: 0.8125rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding: 0.55rem 1.15rem;
    border: 1.5px solid var(--color-border-medium);
    border-radius: var(--radius-xs);
    color: var(--color-navy);
    transition: all var(--transition-fast);
  }

  .btn-header-inquire:hover {
    border-color: var(--color-navy);
    background-color: var(--color-navy);
    color: #FFFFFF;
  }

  .btn-header-wa {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-display);
    font-size: 0.8125rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding: 0.55rem 1.15rem;
    border: 1.5px solid var(--color-orange);
    border-radius: var(--radius-xs);
    background-color: var(--color-orange);
    color: #FFFFFF;
    transition: all var(--transition-fast);
  }

  .btn-header-wa:hover {
    background-color: var(--color-orange-hover);
    border-color: var(--color-orange-hover);
    transform: translateY(-1px);
  }

  /* Mobile Toggle */
  .mobile-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
    width: 36px;
    height: 36px;
    padding: 6px;
  }

  .mobile-toggle .bar {
    width: 100%;
    height: 2px;
    background-color: var(--color-navy);
    transition: transform var(--transition-fast), opacity var(--transition-fast);
  }

  .mobile-toggle.active .bar:first-child {
    transform: translateY(4px) rotate(45deg);
  }

  .mobile-toggle.active .bar:last-child {
    transform: translateY(-4px) rotate(-45deg);
  }

  /* Mobile Drawer */
  .mobile-drawer {
    display: none;
    position: fixed;
    top: 76px;
    left: 0;
    width: 100%;
    height: calc(100vh - 76px);
    background-color: #FFFFFF;
    border-top: 1px solid var(--color-border);
    overflow-y: auto;
    z-index: 999;
    padding: 2rem var(--gutter);
  }

  .mobile-drawer.open {
    display: block;
  }

  .mobile-nav {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--color-border);
  }

  .mobile-nav-link {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--color-navy);
    text-decoration: none;
  }

  .mobile-nav-link:hover {
    color: var(--color-orange);
  }

  .mobile-actions {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
    margin-top: 2rem;
  }

  .mobile-actions .btn-cib-solid,
  .mobile-actions .btn-cib-outline {
    padding: 0.85rem 1.25rem;
    font-size: 0.8125rem;
    border-radius: var(--radius-xs);
  }

  .mobile-actions .w-full {
    width: 100%;
  }

  .text-center {
    text-align: center;
  }

  .mobile-footer {
    margin-top: 2.5rem;
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .mobile-address {
    font-weight: 600;
    color: var(--color-navy);
  }

  @media (max-width: 1024px) {
    .desktop-nav {
      display: none;
    }

    .header-phone {
      display: none;
    }

    .mobile-toggle {
      display: flex;
    }
  }

  @media (max-width: 640px) {
    .btn-header-inquire {
      display: none;
    }

    .brand-tagline {
      display: none;
    }

    .header-actions {
      gap: 0.65rem;
    }

    .brand-crest-img {
      width: 38px;
      height: 38px;
    }

    .brand-title {
      font-size: 1.15rem;
    }
  }
</style>

<script>
  // Header scroll detection & Mobile drawer toggle
  const header = document.getElementById('mainHeader');
  const toggleBtn = document.getElementById('mobileMenuToggle');
  const drawer = document.getElementById('mobileDrawer');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 20) {
      header?.classList.add('scrolled');
    } else {
      header?.classList.remove('scrolled');
    }
  }, { passive: true });

  toggleBtn?.addEventListener('click', () => {
    const isExpanded = toggleBtn.getAttribute('aria-expanded') === 'true';
    toggleBtn.setAttribute('aria-expanded', (!isExpanded).toString());
    toggleBtn.classList.toggle('active');
    drawer?.classList.toggle('open');
    document.body.style.overflow = !isExpanded ? 'hidden' : '';
  });

  // Close drawer on link click
  drawer?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      toggleBtn?.setAttribute('aria-expanded', 'false');
      toggleBtn?.classList.remove('active');
      drawer?.classList.remove('open');
      document.body.style.overflow = '';
    });
  });
</script>
